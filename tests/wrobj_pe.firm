obj code {
    section = ".text";
    file    = "tests/test_pe.obj";
}
section out {
    assert obj_lma(code) == 0;
    assert obj_vma(code) == 0;
    assert obj_align(code) == 16;
    wr code;
    assert sizeof(code) == 17;
}
output out;
