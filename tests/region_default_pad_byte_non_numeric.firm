// default_pad_byte must be a numeric value.
// ERR_180: default_pad_byte must evaluate to a numeric value.
region FLASH {
    addr = 0x1000;
    size = 0x100;
    default_pad_byte = "bogus";
}
section top {
    wr8 0x00;
}
output top;
