obj code {
    section = "__text";
    file    = "tests/test_macho.o";
}
section out {
    assert obj_lma(code) == 0;
    assert obj_vma(code) == 0;
    assert obj_align(code) == 16;
    wr code;
    assert sizeof(code) == 18;
}
output out;
